#include "Server.hpp"

Server::Server(int port) : ServerBase(port) {
  // Developer implementation
}

int Server::add(int a, int b) {
  // Developer implementation
  return a + b;
}

int Server::add(int a, int b) {
  // Developer implementation
  return a * b;
}